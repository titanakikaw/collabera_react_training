// import { x, y } from "./app";
import { x, y } from './app';
import Box from './test';
import '../style.scss';

function add(a, b) {
  return a + b;
}

const b = new Box();
b.printType();

console.log(add(x, y));
